// Placeholder so `make test-js` has something to run. It gets deleted once the
// first real piece of app logic (the deadline evaluator) has its own tests.
import { expect, test } from 'vitest'

test('the test runner works', () => {
  expect(1 + 1).toBe(2)
})
