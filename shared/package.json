{
  "name": "@medical/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types for the rules engine, screener, and hours tracker. One source of truth for /functions and /web.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
